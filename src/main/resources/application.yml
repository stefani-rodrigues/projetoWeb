spring:
  application:
    name: projeto-final-web

  datasource:
    url: jdbc:postgresql://localhost:5432/projeto_final_web
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: true